/* UE5 能力账本 —— 喂给模型的「对这个人来说什么便宜什么贵」。
 *
 * 为什么需要这一份：
 *
 * 模型知道 UE5 有哪些功能，但它默认按**一般团队**的成本去估。
 * 对一般团队来说「加三个敌人」是美术排期问题，对他来说是**做不出来**。
 * 反过来「运行时切割网格」对一般团队是技术风险，对他反而是最划算的亮点来源 ——
 * 因为那东西的观感全部来自程序，不来自资源质量。
 *
 * 这个偏差不修，模型给的每一份工作量估算都会系统性地偏。
 * 而工作量恰恰是主人自己说过的「我唯一能真正把关的地方」。
 *
 * 判断标准只有一条，反复用：
 *   **这个东西好不好看，取决于代码，还是取决于买来的资源本身好不好看？**
 *   取决于代码 → 便宜，而且是他的主场。
 *   取决于资源 → 贵，而且是他明确说过永远拼不过的赛道。
 *
 * ⚠️ 这份账本里关于「商城上有没有某类资源」的部分会过时。
 *    模型被要求：涉及具体资源包是否存在、多少钱，一律标成待核实，
 *    不许当成既定事实写进策划书。
 */

export const UE5_LEDGER = `

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【UE5 能力账本 —— 估工作量时必须按这一份，不要按一般团队的常识】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

判断一个方案对他贵不贵，只用这一条：

  **它好不好看，取决于代码，还是取决于买来的资源本身好不好看？**

  取决于代码 → 便宜，而且是他唯一能赢的地方。
  取决于资源 → 贵。他自己写过「在美术上我永远拼不过别人」。

── 一、便宜，而且是他的主场（观感来自程序） ──

这些东西的共同点：效果由代码和参数决定，换一套资源进去照样成立。
**方案的亮点应该优先从这一栏里长出来。**

- **材质与后处理**：描边、色阶、溶解、扫描、色差、屏幕空间扭曲。
  这是杠杆最高的一项 —— 它能把一堆风格不统一的现成资源
  强行拉成同一种视觉语言。对他来说这几乎是唯一的「统一美术风格」手段。
- **Niagara 粒子**：内置模板多，改参数就有效果，不需要美术功底。
  是少数「不会画画也能做出视觉冲击」的东西。
- **Geometry Script / 运行时程序化网格**：切割、生长、形变、拼装。
  这类效果没有任何现成资源能替代，所以它天然是原创的，
  而且完全不需要建模能力。
- **Chaos 物理**：破坏、布料、载具、约束。观感全部来自模拟。
- **PCG / 程序化关卡生成**：用代码换掉关卡美术的工作量。
  一个人做的项目里，这是把内容量做大的唯一现实路径。
- **Mass Entity / 大规模单位**：数量本身就是奇观，且和资源质量无关。
- **纯代码的表现层**：顿帧（time dilation）、镜头位移与切换、
  屏幕震动、时间缩放、后处理瞬间变化。这些都是几十行的事。
- **Control Rig / IK Retargeting**：注意这是**程序化地改现成动画**，
  不是做动画。让买来的动画去适配不同骨骼、加程序化的偏移和反应，
  属于便宜的那一栏。

── 二、贵，或者对他直接不可行（观感来自资源） ──

- ⛔ **任何把核心体验押在角色动画质量上的方案。**
  包括但不限于：动作格斗、招式连段、精确格挡 / 弹反 / 处决、
  潜行的动作演出、需要读招的战斗。
  原因不是做不出功能 —— 顿帧和判定窗口都是纯代码。
  原因是：**这类机制的成败取决于玩家能不能从动画里读出时机**，
  而买来的动画包各个动作前摇长度和辨识度不统一，他又不会改动画。
  机制会因为「读不准」而整个垮掉，且无法通过调代码补救。
- ⛔ **大量独特角色 / 敌人**。每一个都是模型 + 骨骼 + 整套动画。
  设计时按「敌人共用一套骨骼，差异只做在材质、体型缩放、行为和武器上」来想。
- ⛔ **叙事与演出驱动**：过场动画、表演、口型、分镜。全是美术活。
- ⛔ **原创 2D 内容**：立绘、手绘 UI、图标集、漫画式表现。
- ⚠️ **GAS（Gameplay Ability System）**：他明确没学过。
  除非游戏的核心就是技能组合与叠加，否则引入它是净负债 ——
  学习曲线陡，且出问题时很难 debug。默认不要用，要用必须写明为什么值。
- ⚠️ **网络同步 / 多人**：明确没学过，且联调成本极高。默认排除。
- ⚠️ **行为树与复杂 AI**：明确没学过。简单状态机够用就别上行为树。
- ⚠️ **开放世界 / 大地图**：内容量地狱，和 75 人日不兼容。
- ⚠️ **精细 UI 表现**：他自己说缺乏审美，UI 只能照抄现成的。
  不要把亮点押在界面上。

── 三、现成资源的现实（涉及具体资源包时须标「待核实」） ──

- 相对好找：写实向环境与道具、通用人形动画、音效与音乐素材、
  科幻 / 中世纪 / 现代都市这几个常见题材的成套资源。
- 相对难找：风格统一且带专属动画的成套角色、非人形生物的骨骼与动画、
  小众美术风格（手绘、剪纸、像素三维化）的成套资源。
- **最大的坑不是找不到资源，是资源之间风格不统一。**
  五个来源拼在一起会很难看。而他能用的统一手段只有一个：
  用材质和后处理压成同一种视觉语言（见第一栏第一条）。
  所以但凡方案依赖「大量不同来源的资源同屏出现」，就要在这里写清代价。

── 四、用这份账本的规矩 ──

- 任何一个方案都必须能回答：**它的视觉亮点属于第一栏还是第二栏？**
  答不上来，说明还没想清楚这个方案靠什么好看。
- 工作量估算按 **75 人日**这个总盘子来判断，不要按「一年」这个感觉。
- 涉及某个具体资源包是否存在、价格多少，一律写成「待核实」，
  不许当成既定事实 —— 他会照着买。
- 这份账本描述的是**他现在的**能力边界。他明年会学新东西，
  但学习本身要占掉工时，所以「他到时候学一下就行」不是理由，
  要写学习成本，并算进人日。
`
