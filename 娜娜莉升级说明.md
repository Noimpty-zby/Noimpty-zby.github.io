# 娜娜莉升级 · 交付说明

这一批做了三件事：**让她能操控页面**、**让她有时间概念**、**让她能自己在博客里活动**（巡逻、贴表情、发评论、写自己的文章）。

---

## 零、最快路径

```powershell
cd D:\secret\blog

# 1. 把解压出来的文件整体覆盖进来（目录结构一致）
# 2. 填一行：GoatCounter 站点代号
#    _config.butterfly.yml 里搜 NOIMPTY_GC_CODE，把 '' 改成你的代号
# 3. 本地看一眼
npm run server
# 4. 推
git add -A
git commit -m "娜娜莉：页面操控、时间概念、自主巡逻与专栏"
git push origin main
```

然后去仓库 Settings 配好 Secrets（见第五节），手动跑一次 workflow 验证。

---

## 一、她能操控页面了

打开面板直接说：

| 你说 | 她做 |
|---|---|
| `打开归档` `去关于` `带我去 Study` | 跳到对应版块 |
| `打开 GAMES101 作业三` | 按标题跳到那篇文章 |
| `打开 UE5` | 有好几篇都像 → 列出候选让你点 |
| `切换深色` / `浅色模式` | 切主题 |
| `放音乐` `下一首` `暂停` | 控制左下角播放器 |
| `回到顶部` | 滚回去 |

**跳完她会主动问一句**「想知道点什么」，不会跳完就没声了。

两条通路：

- **本地快速通道** —— 上面这些句式她自己就能认出来，**不花一分钱 API**，也不用等模型返回，是瞬时的
- **模型通道** —— 说得含糊时（比如「你自己挑一篇给我看」）才交给模型判断。这时会把站点地图塞给它，并且**限定 url 只能从地图里挑**，防止她编一个不存在的地址

她给出的操作指令写在回复末尾一段隐藏文本里，不会显示给你，也不会存进对话历史（否则她会照着旧指令反复抄）。

---

## 二、她有时间概念了

### 怎么知道你来过

这是这次唯一需要你理解一下的设计。

你的浏览器如果满足**任一条**，就被认定为「主人的浏览器」：

1. 这台浏览器里配过娜娜莉的 API Key（除了你不会有别人配）
2. 你手动开过 `https://noimpty-zby.github.io/#im-noimpty`

认定为主人后，**你的阅读不再计入文章浏览量**，只会向统计服务发一条 `owner-heartbeat` 事件（每小时最多一条）。

这样同时满足了两件事：

- 文章浏览量依然干净 —— 你看多少遍都不影响「有没有同道中人在看」
- 每晚的定时任务能读到这条心跳，知道你多久没来了

> **第 2 条顺手补掉了之前那个手机缺口。** 以前 Umami 的排除方式要在控制台跑代码，手机没法弄。现在手机上开一次那个链接就行，还会弹窗确认。

### 想念邮件

超过 **4 天**没来，晚上那封就不是数据报告了，换成一封短的：她说几句嘴硬心软的话，附上「有几条评论在等你回」「你停在第四章」这类回来的理由。

之后每隔 3 天再发一次，不会天天烦你。想关掉：workflow 里 `MISS_YOU_AFTER_DAYS` 设成 `0`。

---

## 三、她能自己在博客里活动了

新增 `.github/workflows/nanaly.yml`，两个节奏：

### 每天早上 9 点 · 巡逻

她自己逛一遍所有文章，点每个链接、看每张图。**发现坏掉的东西，就到那篇文章的评论区留言提醒你。**

关于误报，我特意处理过：

| 链接类型 | 什么情况才报 |
|---|---|
| 站内链接、图片 | 任何 4xx / 5xx 都报 |
| 站外链接 | **只报 404 和 410** |

站外链接经常对爬虫返回 403 / 429（Epic 文档、Cloudflare 都这样），那不是坏了。**一个天天误报的巡逻只会被你关掉通知**，所以宁可漏报也不吵你。

我做了双向验证：13 篇正常文章 → 0 误报；我造了一篇带坏链接和坏图的文章 → 准确找出两处，并且只在那一篇下面留言。

同一个问题只说一次（评论里埋了隐藏标记判重）。

### 每周日晚 8 点 · 她写自己的文章

她读这一周站上真实发生的事（你提交了什么、读者问了什么、她巡逻看到什么），写一篇 400–700 字的随笔，**提交进仓库触发自动部署** —— 和你自己 push 一篇没有区别。

> 你担心的「文章不在本地就部署不了」，解法就是让她把文件写进仓库。

- 位置：`source/_posts/nanaly-2026-w33.md` 这样的文件名
- 分类：`Life / 娜娜莉`，也就是你说的「一个我的，一个她的」
- 她的文章**默认是公开的**（你的碎碎念仍然上锁）。想让她的也上锁：`tools/nanaly/column.mjs` 顶部把 `PRIVACY` 那两行注释调换一下
- 提交者显示为「娜娜莉」
- 一周只写一篇，文件已存在就跳过

### 贴表情

她会给文章贴表情（❤️ 🎉 🚀 👀），同一篇只贴一次，表情由标题算出来所以每次跑结果一致。手动触发 `react` 才会跑。

### 回评

> ⚠️ **这一段已被第二批更新覆盖，见下面第十节。**
> 现在是**自动回复，不是草稿模式** —— 评论挂满 6 小时、你没回、她也没回，她才接手。
> 日报里仍然会附她拟的回复，方便你复查她说得对不对。

---

## 四、她的发言身份

默认用 `GITHUB_TOKEN`，评论显示为 **github-actions[bot]**，末尾有一行小字署名娜娜莉。

想让她有自己的头像和名字：用一个 **GitHub 小号**的 PAT，存成 Secret `NANALY_GITHUB_TOKEN`。代码会自动优先用它，**不需要改任何东西**。

**不用把小号加成协作者**（理由见第十四节）。这一步完全可选，不做也能跑。

---

## 五、要配的 Secrets

仓库 → Settings → Secrets and variables → Actions

| 名字 | 必需 | 说明 |
|---|---|---|
| `GOATCOUNTER_CODE` | 访问统计要 | 你的站点代号（`xxx.goatcounter.com` 的 xxx） |
| `GOATCOUNTER_TOKEN` | 访问统计要 | **权限只勾 Read statistics 就够** |
| `GMAIL_USER` | 发邮件要 | 你的 Gmail 地址 |
| `GMAIL_APP_PASSWORD` | 发邮件要 | 16 位应用专用密码，不是登录密码 |
| `REPORT_TO` | 可选 | 收件地址，不填就发给自己 |
| `DEEPSEEK_API_KEY` | 强烈建议 | 不配的话她写的东西全变成模板文字 |
| `GISCUS_CATEGORY_ID` | 可选 | 已内置默认值，一般不用配 |
| `NANALY_GITHUB_TOKEN` | 建议 | 她的小号 PAT，见上一节。不配就显示成 github-actions 机器人 |
| `NANALY_LOGIN` | 配了上面就要 | 小号用户名，用来避免她回自己的评论 |
| `OWNER_LOGIN` | 可选 | 填 `Noimpty-zby`。不配会从仓库名推断 |

还要填一个**不是** Secret 的东西：`_config.butterfly.yml` 里的 `NOIMPTY_GC_CODE`。它会出现在网页源码里，本来就是公开的。

---

## 六、先手动跑一次

**别等到定时才发现配错了。**

仓库 → Actions，两个 workflow 都可以手动触发：

- **博客日报** → Run workflow → **勾上 dry** → 只生成不发送，报告在 Artifacts 里下载来看
- **娜娜莉的日常** → Run workflow → 选 `patrol`，**dry 默认是勾上的** → 只打印她会说什么，不真发评论

确认内容没问题，再取消勾选跑一次真的。

---

## 七、我没能验证的部分

诚实说明，免得你以为都验过了：

| 项目 | 状态 |
|---|---|
| 页面操控（全部 12 项） | ✅ 浏览器实测通过 |
| 主人识别与心跳 | ✅ 四种情况实测通过 |
| 巡逻的误报与检出 | ✅ 双向验证（13 篇干净 0 误报，1 篇坏的准确检出） |
| 她的文章能否正确生成并被 Hexo 收录 | ✅ 实测通过（链接、分类、日期都对） |
| 公式/代码渲染、朗读、深度思考 | ✅ 浏览器实测通过 |
| **GoatCounter 真实接口** | ❌ 沙箱连不上外网。按 OpenAPI 规范写的，用假服务验过解析逻辑 |
| **她真的发评论、贴表情** | ❌ 需要真 token。GraphQL 语句按官方文档写，逻辑用演练模式验过 |
| **Gmail 真的发出邮件** | ❌ 需要你的应用专用密码 |

这三项都要等你配好凭据、手动跑一次才知道。**跑挂了把 Actions 的日志截图发我。**

---

## 八、想关掉某一项

| 想关什么 | 怎么关 |
|---|---|
| 想念邮件 | `daily-report.yml` 里 `MISS_YOU_AFTER_DAYS` 设 `0` |
| 巡逻 / 她的专栏 | 删掉 `.github/workflows/nanaly.yml`，或在 Actions 页面把它 Disable |
| 访问统计 | `_config.butterfly.yml` 里 `NOIMPTY_GC_CODE` 改回 `''` |
| 她的文章公开 | `tools/nanaly/column.mjs` 顶部换 `PRIVACY` 那两行 |
| 全部日报 | Actions 页面把「博客日报」Disable |

---

## 九、Umami 怎么办

我已经把 `_config.butterfly.yml` 里的 `umami_analytics.enable` 改成了 `false`。配置整段都留着，想换回去把它改回 `true`、再把 `NOIMPTY_GC_CODE` 清空即可。

cloud.umami.is 上的账号你想留就留，不影响任何东西。

---

# 第二批（自动回评 · 文章批注 · 主动冒泡 · 记忆）

## 十、她会自己回评论了

**默认已开启，不是草稿模式。**

她开口有三条门槛，缺一不可：

1. 这条评论已经挂了 **6 小时**没人管
2. **你还没回过**它
3. **她自己也没回过**（评论里埋了隐藏标记判重）

> 这个「冷静期」是照你说的「我来不及回复的时候」设计的 —— 正常情况下你先回，你忙起来了她才兜底，而不是读者刚发完你还没看见她就抢答。
>
> 想让她更快接手：`nanaly.yml` 里 `NANALY_REPLY_GRACE_HOURS` 改小。改成 `0` 就是随时可能抢在你前面。

**回之前她会先把那篇文章读一遍**，所以答的是「这个博客里写过的东西」，不是泛泛的通用回答。提示词里明确要求：不确定就说不确定、不许编 API 名和数值、别替你许诺什么、对方语气不好也不许对呛。

检查频率：**每两小时一次**，单次最多回 5 条。

我对选择逻辑做了 9 项断言的单元测试，覆盖：该回的两种、冷静期内的、你回过的、她回过的、你自己发的、机器人发的、她自己发的、非文章讨论 —— **全部符合预期**。

## 十一、文章里的批注

她的旁注**直接长在正文里**，读文章时不用打开面板就能看到她。构建时嵌入，没有额外的 JS，也不会闪。

两个刻意的设计：

- **定位用锚点文字，不用段落序号。** 序号会因为你在前面插一段话就全部错位，批注会贴到不相干的地方去。**匹配不上就干脆不插** —— 宁可少一条，也不要贴错位置
- **含公式的段落不选。** 锚点是纯文本，而渲染后那里是 KaTeX 的 DOM，两边对不上会静默丢失

正文改过才重新生成（按内容哈希判断），所以批注不会天天变，也不会白烧 token。

**上线后要手动跑一次才有批注**（我不能拿假模型写的内容当成她的话交付给你）：
Actions → 娜娜莉的日常 → Run workflow → 选 `notes` → **取消勾选 dry**。

## 十二、她会主动冒泡了

你在一篇文章上待够时间、并且确实在往下读，猫爪按钮上会冒红点，旁边浮出一句话 —— 内容跟你**正在读的那个小节**有关。

三条克制原则：**每篇最多打扰一次**、面板开着就不出现、只在读到 22%–94% 之间才开口（开着页面去泡茶不算）。

点那个气泡会直接开面板并把话接上。

## 十三、她记得你

存在**你这台浏览器**里，不上传任何地方。记的是：

- 你问过什么（最近 40 条）
- 你在哪篇文章上问的
- 你反复回看哪几篇

下次对话时压成一小段塞给她，让她能接上话。比如你在同一篇上问了 4 次以上，她会知道你多半卡在那儿了。

控制台里可以查看和清空：

```javascript
NANALY.memory()         // 看她记了什么
NANALY.forgetMemory()   // 全部忘掉
NANALY.poke()           // 手动触发一次冒泡，用来看效果
```

## 十四、这一批要你做的

| # | 事情 | 说明 |
|---|---|---|
| 1 | 小号建 classic PAT | scope 勾 `public_repo` + `write:discussion`，存成 Secret `NANALY_GITHUB_TOKEN` |
| 2 | 小号用户名存成 Secret | `NANALY_LOGIN`，用来避免她回自己的评论 |
| 3 | 上线后手动跑一次 `notes` | 否则没有批注 |
| 4 | 顺手把 `OWNER_LOGIN` 也配上 | 值填 `Noimpty-zby`。不配的话代码会从仓库名推断，一般也对 |

**不用把小号加成协作者。** 加了她的评论会顶个「Collaborator」标签，读起来像你雇的员工；不加就是个普通用户，更像住在这儿的一只猫。公开仓库任何登录用户都能评论，权限够用。

---

# 第三批（DeepSeek V4 迁移）

## 十五、模型名和思考模式都变了

你反馈「关掉深度思考后还在思考」，查下去发现根子在 DeepSeek 那边：

1. **`deepseek-chat` 和 `deepseek-reasoner` 已于 2026-07-24 停用**，现在是 `deepseek-v4-flash` 和 `deepseek-v4-pro`
2. **思考模式不再靠换模型名，改成了参数** `thinking: {type: enabled|disabled}`
3. **而且默认是开着的** —— 不显式写 `disabled`，它就一直思考，一直按思考的量收钱

我原来的实现是「换模型名」，在新体系下等于没关。这就是那个 bug。

现在：

| 开关 | 模型 | 参数 |
|---|---|---|
| 关（默认） | `deepseek-v4-flash` | `thinking: disabled` + `temperature: 0.8` |
| 开 | `deepseek-v4-pro` | `thinking: enabled` + `reasoning_effort: high`（不发 temperature，思考模式不支持） |

**本机存着的旧模型名会自动升级**，你不用手动改设置。

**服务端也一样修了** —— 日报、批注、回评原来都在用停用的名字，而且因为默认开思考，一直在为你看不到的推理内容付费。现在显式关掉了。

价格参考（每百万 token）：`v4-flash` 输入 $0.14 / 输出 $0.28；`v4-pro` 输入 $0.435 / 输出 $0.87，**贵大约三倍**。

## 十六、深度思考现在看得见了

开着的时候副标题会显示「**深度思考中 · 正在读：xxx**」。

它是个会记住状态的开关，你不关就一直开着 —— 但至少现在你能一眼看见自己开着，不会糊里糊涂烧钱。

高级设置里多了一项「思考深度」，可填 `low` / `high` / `max`。

---

# 第四批（资讯板块 · 日程表）

## 十七、资讯：三天一期，她自己去搜

新菜单 **资讯** → `/news/`。每三天北京时间上午 10 点自动跑一次。

四个主题，名额按你说的「最想知道」分配：

| 主题 | 每期最多 |
|---|---|
| 行业职场动态 | 3 条 |
| 面试与求职干货 | 3 条 |
| UE5 与引擎学习 | 2 条 |
| 游戏与实况 | 2 条 |

**两条硬规矩，决定这个板块是有用还是垃圾：**

1. **每条必须挂来源链接。** 她转述的东西你得能一键去核对
2. **宁缺毋滥。** 提示词里明确写了：纯宣传稿、标题党、蹭热点的一条都别留，实在没值得写的就输出「这几天没什么值得说的」，那也算合格

另外还做了**跨期去重** —— 会读最近四期用过的链接，同一条新闻不会连着几期反复出现。

### 它不进首页和 RSS

按你选的方案，这些内容做成 Hexo 的 **page** 而不是 post，所以天然不出现在首页、归档、RSS 和站内搜索里。我实测确认过四处都干净。

理由写在 `/news/` 页面上了：这是自动整理的二手信息，三天一篇会很快把你那些几万字的原创复盘冲淡。

**要多配一个 Secret**：`TAVILY_API_KEY`（和娜娜莉浏览器里那把是同一个）。

## 十八、日程表

新菜单 **日程** → `/schedule/`，**已上锁**（走你原来那套暗号，外人看不到）。

月视图日历，点某一天就能写安排、勾完成、删除。改完点「保存到仓库」。

### 数据存在哪

```
真身    source/_data/schedule.json    ← 仓库里的唯一真相，晚上的日报也读它
浏览器读 /schedule/data.json           ← 构建时从真身生成的副本
浏览器写 GitHub Contents API           ← 直接改真身，随后自动部署
```

保存时会带上文件的 `sha`，所以**不会覆盖掉别处的改动** —— 如果远端更新了，它会明确告诉你去刷新，而不是默默盖掉。

### 那个 token

存在娜娜莉的**加密保险箱**里（AES-GCM + PBKDF2，要密码解锁），和 DeepSeek key 同一套机制。设置面板里多了一个「GitHub Token」字段。

**建 token 时权限压到最小：**

<https://github.com/settings/tokens?type=beta> → Generate new token

- Repository access：**Only select repositories** → 只勾你的博客仓库
- Permissions → Repository permissions → **Contents: Read and write**
- 其余**一个都别给**

最坏情况：别人同时拿到你的电脑**和**解锁密码，能改这一个仓库。改不了别的仓库，也动不了你的账号。

### 邮件提醒

每晚的日报最上面会多一张「今天的安排」卡片：

- 今天几件、做完几件（标题上直接显示 `2/3`）
- 逐条列出，做完的划掉
- **还欠着的**（过去两周内没勾完成的）单独用红框标出来
- 明天有什么，预告一句

娜娜莉的开场白也会带一句 —— 但只催一句，不唠叨。

### 注意

- 改完**一定要点保存**，不点就只在你这台浏览器里
- 保存后站点要 1–2 分钟重新部署，当晚的邮件才读得到
- 没解锁保险箱就点保存，会明确提示你去解锁，不会静默失败

## 十九、日程的自动完成

默认还是**手动勾**。但每条任务可以挂一个**可选的完成条件**，满足了她就替你勾上。

点任务下面的「＋ 加个完成条件」，三选一：

| 条件 | 判定依据 | 例子 |
|---|---|---|
| **发布了文章** | 窗口内新增的文章，标题或文件名含关键词 | 「完成作业二复盘」→ 关键词填 `作业二` |
| **改动了文章** | 窗口内 git 改过的文章文件名含关键词 | 「补 Bézier 的公式」→ 填 `homework-four` |
| **回复了评论** | 你在 Discussions 里回过（关键词留空 = 任意一条） | 「回一下那条评论」 |

**「复习光栅化」这种没有任何客观信号能证明的，别挂条件** —— 挂了也判不出来。界面上默认就是手动，不用管。

### 她勾了会告诉你

每晚的邮件里，被自动勾上的那条底下会有一行小字：

> ✓ ~~完成作业二复盘~~
> 　窝替你勾的 —— 你发了《GAMES101 作业二：三角形光栅化与 Z-Buffer》

不是今天那一格的，会单独列在下面。**判错了你自己去掉勾就行**，她不会再勾回来（那天的信号已经过去了）。

### 三条边界

我做了六种情况的双向验证，确认：

- 挂了条件且信号命中 → **勾**
- 挂了条件但信号没命中 → **不勾**
- **没挂条件 → 永远不碰**，哪怕内容看起来很像
- 本来就完成的 → 保持原样，不重复记录

### 多一个权限

`daily-report.yml` 的权限从 `contents: read` 改成了 `contents: write`（她要把勾上的结果提交回去），另加 `actions: write`（提交完叫一次部署）。
