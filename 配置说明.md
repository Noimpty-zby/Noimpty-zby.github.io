# 配置说明

这一版做了三件事：**博客框架重构**、**全站上锁**、**点子页重做成策划室**，
顺带修了你列的那几个 bug。

下面按「你要动手做什么」排序，先做完第一节，站点就能正常跑起来。

---

## 零、怎么把这一版换上去

### 第 1 步：复制

把压缩包里 **`博客仓库\` 的内容**（不是这个文件夹本身，是里面的东西）
拖进 `D:\secret\blog`，选「替换目标中的文件」。

`node_modules\` 和 `source\music\` 里的 mp3 都不会被动 —— Windows 只覆盖同名文件，
不会删掉只存在于目标里的东西。`package-lock.json` 没变，所以**不用重新 npm install**。

### 第 2 步：删旧文件 ← 这一步不能跳

**Windows 的「替换」不会删除文件。** 新版里已经删掉的那些旧文件会安静地留在原地，
其中 6 个是页面（`source\study\` 那四个、`source\ideas\`、`source\ideas-vault\`）。

留着它们的后果我实测过：**不报错、不泄漏、构建全绿、部署成功**，
但那 6 个旧页面会照常生成，**把新版给它们准备的跳转页顶掉** ——
旧地址不再跳转，站上多出 6 个僵尸页面，而你不会发现。这是最难查的那类错。

所以别手删。在 `D:\secret\blog` 里跑我放进去的脚本：

```powershell
cd D:\secret\blog
.\升级清理.ps1
```

它会列出要删的东西、问你一句、删掉，然后顺手清掉构建缓存（`db.json` 和 `public\`），
最后检查一下音乐文件还在不在。

> **PowerShell 拦着不让跑脚本的话**，用这两句最省事（不用改系统策略）：
>
> ```powershell
> Unblock-File .\升级清理.ps1                        # 去掉「此文件来自网络」的标记
> powershell -ExecutionPolicy Bypass -File .\升级清理.ps1
> ```
>
> 想改当前窗口的策略也行，注意 `Bypass` 是 `-ExecutionPolicy` 的**值**不是开关：
> `Set-ExecutionPolicy -ExecutionPolicy Bypass -Scope Process`

### 第 3 步：本地验一遍

```powershell
npm test           # 5 个测试文件全过
npm run build      # 构建
npm run linkcheck  # 零死链
npx hexo server    # 打开 http://localhost:4000 看看
```

**现在就能验的**：三个入口弹暗号框、输暗号能进、课外里有两门课各自的文章、
旧地址会跳转、娜娜莉知道今天几号和有几篇文章。

想让站内搜索也能用，构建前先设一下暗号：

```powershell
$env:NOIMPTY_PASSPHRASE = "你的暗号"
npm run build
```

**填错了会有醒目的黄色警告**（我加了一道对比校验），照着改就行 ——
不加这道校验的话，填错的后果是「你自己的暗号打不开自己的站」，而且完全看不出来。

### 第 4 步：确认无误再推

```powershell
git status         # 扫一眼，改了什么删了什么一目了然
git add -A
git commit -m "板块重构 + 全站上锁 + 策划室"
git push
```

推上去之后 Actions 会跑：测试 → 构建 → 空页面检查 → 死链检查 → 上锁自检 → 部署。
任何一步红了都不会发出去。

---

## 一、先做这些（不做站点会有功能缺失）

### 1. 设一个 secret：`SITE_PASSPHRASE`

**这一步不做的后果**：站内搜索用不了（娜娜莉的「全站搜一下」会报错）。站点本身正常。

去 `Settings → Secrets and variables → Actions → New repository secret`：

| 名字 | 值 |
|---|---|
| `SITE_PASSPHRASE` | 你现在用的那个暗号（进 Life / 课内 / 课外 时输的那个） |

**为什么需要它**：`search.xml` 是全站所有文章的**完整正文**，一个 URL 就能下完，
是软锁里最大的一个口子。现在构建时会用这个暗号把它整体 AES 加密，
你解锁之后浏览器再解回来。没有这个 secret，构建会把它清空（不漏，但也搜不了）。

**注意**：以后改暗号，只改这一个地方，然后重新部署一次。
代码里的校验哈希现在是从这个 secret 派生的，不用手动同步 —— 这是这次顺手修掉的一个坑。

### 2. 建策划室的私有仓库

如果 `Noimpty-zby/noimpty-ideas` 还在，清空它重新来（旧的点子结构不兼容）。

把 `私有仓库模板/` 里的文件传进去：

```
charter.md                 ← 从模板复制，然后填
state.json                 ← 原样复制
feedback/inbox.json        ← 把 feedback-inbox.json 改名放进 feedback/ 目录
README.md                  ← 原样复制，是给你看的说明
```

然后**把 `charter.md` 填了**。这是整套东西唯一的地基 ——
填得含糊，产出就是通用废话。模板里每一节都写了怎么填。

如果你现在还想不清楚要做什么游戏，那就在「我想做的方向」里写「还没想好，帮我找」——
这是一个合法且诚实的答案，探索阶段本来就是干这个的。

### 3. 配策划室的 secret

| 名字 | 去哪拿 | 不配的后果 |
|---|---|---|
| `ANTHROPIC_API_KEY` | <https://console.anthropic.com> → API Keys | **不配也能跑**，自动降级到 DeepSeek，日志第一行会写明用的是哪个。等你拿到卡再补上，代码一个字都不用改 |
| `IDEAS_REPO` | 填 `Noimpty-zby/noimpty-ideas` | 策划室整个跳过 |
| `IDEAS_TOKEN` | 见下 | 同上 |
| `TAVILY_API_KEY` | 你应该已经有了（资讯板块在用） | 探索阶段不检索，只能凭模型自己的知识 |

**`IDEAS_TOKEN` 怎么拿**：
GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens

- Repository access：**只勾** `Noimpty-zby/noimpty-ideas`
- Permissions → Repository permissions → Contents：**Read and write**
  （写权限是给反馈用的 —— 你在页面上写的反馈要 PUT 回私有仓库）
- 有效期建议一年，到期前 GitHub 会发邮件

**Anthropic 充多少**：先充 5 美元。一周三次，一次几万 token，够跑很久。

### 4. 浏览器里的保险箱要更新 token

策划室页面要**写**反馈，所以你存在娜娜莉保险箱里那把 GitHub token
需要对私有仓库有 **Contents: Read and write**（以前只读也能看点子）。

用上面那把 `IDEAS_TOKEN` 就行：博客上点左下角猫爪 → 齿轮 → 填进 GitHub Token 那一栏。

---

## 二、博客框架变成什么样了

### 新的板块结构

```
/                    首页：大图 + 三个入口（core / extra / life）。**唯一对外的页面**
├── /in-class/       自学课内（锁）
│   ├── /dsa/        数据结构与算法 — Abdul Bari
│   ├── /csapp/      CMU 15-213 CSAPP
│   ├── /nju-os/     操作系统：设计与实现 — 南大 蒋炎岩
│   └── /cs144/      计算机网络 — Stanford CS144
├── /extra/          自学课外（锁）
│   ├── /games101/           GAMES101 — 闫令琪        ← 原有 8 篇已迁入
│   ├── /ue5-looman/         UE5 C++ — Tom Looman     ← 原有 6 篇已迁入
│   └── /ue5-ulibarri/       UE5 C++ — Stephen Ulibarri（新开，空）
├── /life/           Life（锁）                        ← 2 篇
├── /studio/         策划室（锁）← 原来的「点子」页
├── /news/           资讯（锁）
├── /schedule/       日程（锁）
└── /about/          关于（锁）
```

### 旧地址怎么办

**文章本身的永久链接没变** —— 它是 `:year/:month/:day/:title`，这次只改了分类，
所以你以前发出去的文章链接、别人的收藏，全部照样能打开。
文章正文里的图片和交叉引用也都没受影响（全站 5326 个站内链接扫过，零死链）。

变的是**板块页和分类页**的地址。这些都加了跳转（`scripts/noimpty-redirects.js`）：

| 旧地址 | 跳到 |
|---|---|
| `/study/` | `/extra/` |
| `/study/games101/` | `/extra/games101/` |
| `/study/ue5/` | `/extra/ue5-looman/` |
| `/study/competition/` | `/studio/`（比赛的事现在归策划室管） |
| `/ideas/` | `/studio/` |
| `/ideas-vault/` | `/studio/` |
| `/categories/study/` 及其子页 | `/categories/extra/` 对应页 |
| `/categories/ideas/` | `/studio/` |

跳转页是独立的最小 HTML，**不加载暗号门** —— 所以它会先跳，然后在目的地被拦。
这样既不会泄漏内容，也不会让你自己被卡在一个空白的跳转页上。

以后再改文件名或板块名，往那张表里加一行就行。

### 文章的分类怎么变的

front-matter 里的 `categories` 从 `[Study, GAMES101]` 改成了 `[课外, GAMES101]`。
以后写新文章，按这个填：

| 写什么 | categories |
|---|---|
| GAMES101 笔记/作业 | `- [课外, GAMES101]` |
| Tom Looman 的 UE5 课 | `- [课外, UE5-Looman]` |
| Stephen Ulibarri 的 UE5 课 | `- [课外, UE5-Ulibarri]` |
| 数据结构与算法 | `- [课内, DSA]` |
| CSAPP | `- [课内, CSAPP]` |
| 操作系统 | `- [课内, NJU-OS]` |
| 计算机网络 | `- [课内, CS144]` |
| 生活 | `- Life` |

另外每篇都要加这三行（全站上锁需要）：

```yaml
privacy: protected
sitemap: false
private_section: 课外        # 或 课内 / Life，决定解锁框上显示哪个板块名
```

### 关于两门 UE5 课的名字

你写的是「Unreal Engine 5 C++ The Ultimate Developer Course——Tom Looman」，
但你现有的文章讲的是 ActionRoguelike 项目，那是 Tom Looman 的
《Professional Game Development in C++ and Unreal Engine》。
「The Ultimate ... Course」系列是 Stephen Ulibarri 的。

我按这个理解建的板块。如果你觉得名字不对，改这两个文件的标题就行：
`source/extra/ue5-looman/index.md`、`source/extra/ue5-ulibarri/index.md`。

---

## 三、全站上锁做到了什么程度

你选的是**前端软锁**。下面是它挡得住和挡不住的，说清楚边界比说「已上锁」有用。

### 挡得住

| 途径 | 处理方式 |
|---|---|
| 直接访问任何页面 | **除首页外全部**弹暗号框（默认拒绝：新加板块自动被锁，不用记着加清单） |
| 归档 `/archives/` | 锁，包括按年、按月的子页 |
| 分类 `/categories/` `/categories/extra/games101/` | 锁，每个分类页单独进清单 |
| 标签直连 `/tags/xxx/` | 锁，每个标签页单独进清单 |
| 侧边栏「最新文章 / 分类 / 标签 / 归档」 | **从构建源头关掉**，不是用 JS 藏 —— 藏起来的东西还在 HTML 里 |
| 首页文章列表 | 构建时清空，首页只剩大图 + 三个入口 |
| RSS / Atom | 删除，构建时还有一道兜底会移除它 |
| sitemap.xml | 删除 |
| 搜索引擎 | `robots.txt` 拒绝全站 |
| **search.xml** | **AES-256-GCM 加密**，密钥由暗号经 PBKDF2 派生 |

### 挡不住

**打开任何一个上锁页面，按 F12 或「查看源代码」，文章正文就在 HTML 里。**

这是软锁的固有边界，不是没做好。要真正挡住，只有两条路：

- **构建时把正文也加密**，浏览器凭暗号解密。改动量中等，`search.xml` 这套加密逻辑可以直接复用。
- **仓库转私有 + 换 Cloudflare Pages / Vercel**，靠平台鉴权。GitHub Pages 免费版发布不了私有仓库。

哪天想升级，架构不用重做 —— 现在的锁清单和密钥派生都是现成的。

### 每次部署会自检

`pages.yml` 里加了一步「上锁自检」，下面任何一条不过就**直接让部署失败**：

1. 锁清单生成了、不是空的
2. `atom.xml` / `sitemap.xml` 不存在
3. `robots.txt` 拒绝全站
4. `search.xml` 不是明文
5. 首页上没有任何文章链接
6. 公开页面（现在只有首页）上没有出现课程名、技术栈、娜娜莉这类会暴露内容的词

另外还加了一步 **站内死链检查**（`npm run linkcheck`）：
扫每个 HTML 里的每个 `href`/`src`，解析不到文件就让部署失败。

加这一步是因为我自己在这次重构里就漏了一次 —— 板块页删掉之后忘了加跳转，
而这种错不会报任何异常，只会在有人点下去的时候 404，
而那个人多半永远不会告诉你。它顺带还揪出了另一个：
`hexo-generator-feed` 就算配置整段注释掉，也会往每个页面头部塞一条
指向 `/atom.xml` 的订阅链接 —— 75 个页面各挂一条死链，同时给爬虫指了条路。
现在在渲染后直接抹掉了。

这一步的意义在于：上面这些漏洞在页面上**完全看不出来**，只有直接请求那几个文件才会发现。

---

## 四、策划室怎么用

### 它和原来的「点子」有什么不同

| | 原来的点子 | 现在的策划室 |
|---|---|---|
| 干什么 | 帮你找别人的玩法参考 | **自己立项，写完整策划书** |
| 产出 | 一篇篇独立的资料整理 | 一个个项目文件夹，8 份互相衔接的文档 |
| 记忆 | 只记「引用过哪些网址」 | 全局状态机 + 每个项目的修订历史 |
| 你的角色 | 读者 | **甲方** —— 你给反馈，它据此修订 |
| 会不会停 | 不会 | 会。累积负面反馈到阈值就做停更评估 |

### 一次跑做什么

一周三次（周一 / 周四 / 周六晚上 9 点）。每次**只做一件事**，按优先级：

1. **有你的反馈没处理** → 修订。你的输入永远排第一。
2. **某个项目该停了** → 停更评估。
3. **有立项、文档没写完** → 深化，写下一份。
4. **有 4 星以上的候选、活跃项目不满 2 个** → 立项。
5. **什么都没有** → 探索新方向。

### 八份文档

| 文件 | 内容 | 这一份最要命的一节 |
|---|---|---|
| `00-pitch.md` | 立项书 | 「我的判断」—— **允许写「这个方向不值得做」** |
| `01-pillars.md` | 设计支柱 | 「违反它的例子」—— 举不出来说明支柱太宽泛 |
| `02-core-loop.md` | 核心循环 | 分钟级循环。这一层写不具体，整个游戏就是空的 |
| `03-systems.md` | 系统设计 | 「砍掉这个系统，游戏还成立吗」 |
| `04-content.md` | 内容与进程 | 内容量**总人日**。这个数字通常会吓到人，那正是它的作用 |
| `05-tech.md` | 技术方案（UE5） | 「做不出来的退路是什么」 |
| `06-production.md` | 生产计划 | **每个里程碑的取消条件** |
| `07-competitive.md` | 竞品与风险 | 致命假设，最多三条 |

一个项目大概三周写完。中间你的反馈会插队 —— 那是设计如此，反馈比产量重要。

### 反馈怎么给

策划室 → 打开某个项目 → 打开某份文档 → 拉到底，有个反馈框。

四个评价：**很有搞头 / 一般 / 可行性差 / 停掉**，加一段文字说明。

最有价值的不是评级，是**具体哪一句让你觉得不对**。

> ✗「可行性差」
> ✓「第三节那个四种武器形态我做不出来，光动画就要 12 套，我不会做动画」

下次跑的时候它会逐条回应，改写那份文档，并把「怎么改的」写进 `CHANGELOG.md`。

### 几件你应该知道的事

- **它可以不同意你。** 提示词里写死了：如果它认为你的反馈判断有误，可以不改，
  但必须写清楚理由。一个唯唯诺诺的主策划没有用。
- **交白卷是合格输出。** 探索没找到值得写的方向时，它会明说，而不是硬凑四个。
- **停更不是失败。** `POSTMORTEM.md` 里会写「什么条件下这个想法会重新成立」——
  停掉的项目也是资产。
- **同时最多两个活跃项目。** 一个人的注意力就这么多，三个并行等于三个都停在半路；
  但只有一个的话，卡住时没地方去。想改：工作流里的 `STUDIO_MAX_ACTIVE`。

### 手动跑

Actions → 策划室 → Run workflow。
- `what` 选 `auto` 就按优先级自己决定，也可以强制某个动作
- `dry` 默认勾上（**只演练不写**，产出打印在日志里）—— 第一次强烈建议先演练一次

---

## 五、你列的那几个 bug，分别是怎么回事

### 1. 娜娜莉的时间认知错乱

**你看到的**：她说「周四喵，8 月 13 号」，实际是 8 月 15 号周六。

**原因**：**没人告诉过她现在几点。** 系统提示词里一个字的时间信息都没有。
模型没有时钟 —— 它只能从训练数据和日程页的内容里凑一个日期出来。

**改了什么**：每一轮对话都在系统提示词里塞一条真实的北京时间 + 星期，
并明确要求「任何涉及日期的回答都必须从这一行算起，算之前先在心里减一遍」。

### 2. 分不清 Ideas 板块和点子页，指出后还是跳错

**原因**：站点地图里有**两条**记录都带 `ideas` / `点子` 别名 ——
一个是 `/ideas/` 板块，一个是 `/ideas-vault/`。相似度打分完全一样，
她在两个之间反复跳错，而且被指出来之后**还是**跳错，因为她根本没有可以区分的信息。

**改了什么**：`/ideas/` 板块删掉了，「点子 / 策划」这一组别名现在只指向 `/studio/` 一条记录。
不给歧义留位置。

### 3. 数错文章数量（说 6 篇，实际 16 篇）

**原因**：同上 —— 她手上没有文章清单。以前只有在你问「打开某某页面」时才会加载站点地图，
平时问「有几篇文章」，她只能凭印象编一个。

**改了什么**：每一轮对话都带上**完整的文章清单和总数**（标题 + 板块 + 日期），
并写明「报这个数字，不要自己数、不要凭印象」。
读不到索引时（比如还没解锁）会明说读不到，**不许猜一个数字**。

### 4. 资讯的来源链接指不到真出处

**原因**：这块之前修过一次「模型自己编网址」，改成了「她只写编号、链接由代码拼」。
那一层挡住了编造，但挡不住**编号张冠李戴** —— 她写「A 公司裁了引擎组」却标了 `[3]`，
而 `[3]` 是一条讲面试的。链接能点开、域名也真，点进去才发现文不对题。

根源是模型在长素材列表里数错行，靠改提示词压不住。

**改了什么**：加了一道内容校验（2-gram 重合度）：

- 对得上 → 照常挂
- 对不上、但另一条明显更像 → **改挂那一条**，日志里说明改过
- 对不上、也没有更像的 → **整条丢掉**

顺带把域名写进链接文字（`[来源 · gamedeveloper.com]`），万一还有漏网的，一眼能看出不对劲。

### 5. 加密文章从归档 / 分类 / 标签泄漏（日报天天报 5 处）

这一条**当时是真的漏了**，而且现在的修法和你想的不一样。

原来的检查问的是「受保护的那几篇有没有漏进公开索引页」—— 那时候大部分内容是公开的。
现在全站上锁之后，`/archives/` 自己也在锁后面，它里面有文章链接**不算泄漏**。
拿旧问法去查，它会一直报 5 处，全是误报。

**改了什么**：泄漏本身由「全站上锁」解决了（见第三节）。
日报的检查也跟着重写了，现在问的是四件事：
真正公开的那两个页面上有没有文章链接、feed/sitemap 在不在、
`search.xml` 加密了没有、`robots.txt` 拒绝爬虫了没有。

### 6. 娜娜莉随笔的标题被截断

**你看到的**：一篇标题叫
《窝在服务器角落看了三天提交记录，主人像只被 UE5 追着跑的仓鼠。日程更新从 8 月 13 日一路滚到 16 日，中间还》
的文章 —— 正好 60 个字。

**原因**：老代码是「输出的第一行当标题，`slice(0, 60)`」。
她那次没按格式写、直接从正文开始，于是正文第一段被当成标题砍断，
**而且那一段同时从正文里消失了** —— 标题把它吃掉了。

**改了什么**：改成显式分隔符（`标题：` + `===正文===`）加三道校验：
超过 30 字 / 带句号 / 带换行，任何一条不过就整篇丢掉。
宁可这周不发，也不发一篇标题坏掉的。加了单元测试守着。

那篇已经发出去的，标题我改成了《主人像只被 UE5 追着跑的仓鼠》。
被吃掉的那一段找不回来了（它从来没被写进文件）。

### 7. 她提到的「资讯零字节」是什么

那是**之前已经修好的一个 bug**，她在随笔里提了一嘴而已。

成因是给 Hexo 的 page 加了 `layout: post`，Butterfly 的模板去读 `page.categories.data`
抛了 TypeError —— 而 `hexo generate` 照常退出 0，只是把那个页面写成了 0 字节。
工作流全绿、文件在仓库里、点进去一片空白。

现在部署流程里有一步专门查 0 字节页面，查到就让构建失败。这次也验证过了，没有空页面。

### 8. 她提到的「RSS 问题」

她那句话是「窝在 RSS 里翻的时候眼睛舒服了不少」，不是在报 bug。

不过现在 RSS 已经整个关掉了 —— feed 是「把内容主动推出去」，和上锁是相反的方向，
就算按 privacy 过滤，只要有一篇漏标就是全文外泄。

### 9. 她提到的「日程提交时区问题」

**查下来日程本身没有时区 bug** —— `schedule.js` 和 `schedule-auto.mjs` 全程走 `Asia/Shanghai`，
是对的。她那段话是编的，因为当时她根本不知道今天几号（见第 1 条）。

不过顺手查出来一个**真的**时区问题，在别的地方：她写随笔时判断「这周写过没有」
用的是本机时区，而机器跑在 UTC。跨年跨周那几天会把「这周」算错一周，导致重复写或者漏写。
已经改成按北京时间算，加了测试。

### 10. AI 日报的漏洞

「加密文章泄漏 5 处」不是日报的 bug，是它**如实报告了一个真实存在的问题**（见第 5 条）。
它做对了。

日报本身改的是那一项检查的内容 —— 因为全站上锁之后，旧的问法已经不成立了，
不改的话它会一直报同一条误报，而误报报久了你就不看它了，那才是真正的损失。

---

## 六、你还问了：要加什么 plugins / skills / MCP

先说结论：**这套东西现在不需要额外的 MCP。** 但有几个方向值得考虑，按性价比排：

### 值得加的

**1. Anthropic 的 Web Search 工具（替代 Tavily）**

现在探索阶段用 Tavily 搜。Claude 的 Messages API 支持内置的 `web_search` 工具，
让模型**自己决定搜什么、要不要再搜一轮**，比现在「先出词、再搜、再判断」这种固定流程更聪明。

代价：按次计费（约 10 美元 / 1000 次搜索），而且要改 `tools/studio/llm.mjs`。
建议先用现在这套跑几轮，觉得检索质量不够再换。

**2. 让它读得到你的 UE5 工程**

这是**提升最大的一个方向**，因为策划书里最容易脱离实际的就是技术方案和工作量估算 ——
它现在只能靠你在 `charter.md` 里写的自述来猜你的水平。

做法有两种：
- **轻量**：在 `charter.md` 里贴你 ActionRoguelike 工程的目录树和几个关键类的头文件。
  改一次文件的事，效果立竿见影。
- **正经**：给策划室加一步「读 GitHub 上的工程仓库」，用现成的 `store.mjs` 那套 API 就行。
  如果你的 UE5 工程在 GitHub 上（哪怕私有），这个很值得做。

**3. Notion / Obsidian MCP（如果你用）**

策划书现在只在私有仓库里。如果你平时的笔记在 Notion 或 Obsidian，
接一个 MCP 让它把定稿同步过去，你在手机上也能翻。
但这是锦上添花，不影响核心。

### 不建议加的

- **各种「游戏开发」MCP**：现在市面上的基本是包装过的搜索，Tavily 已经覆盖了。
- **图像生成**：策划阶段画概念图听起来很美，实际上会把注意力从机制拉到表现上。
  你现在缺的不是图。
- **更多的模型**：一个够强的模型 + 好的提示词 >> 三个模型互相投票。

### 真正决定质量的不是工具

按影响从大到小：

1. **`charter.md` 写得有多具体** —— 这一条的影响超过其他所有加起来
2. **你的反馈有多具体** —— 「第三节那个数值我做不出来」vs「可行性差」
3. 模型选型（Claude vs DeepSeek）
4. 检索质量
5. 其他

所以如果你只想在一件事上花时间，花在填 `charter.md` 上。

---

## 七、验证清单（我这边跑过的）

- ✅ `npm test` —— 5 个测试文件，84 项断言全过（其中 44 项是这次新加的：随笔标题 12、资讯来源 13、策划室决策 19）
- ✅ `hexo generate` —— 323 个文件，0 个空页面
- ✅ 上锁自检 —— 锁清单 87 条、无 feed/sitemap、robots 拒绝全站、search.xml 已加密、首页无文章链接
- ✅ 无头浏览器实测 —— 19 个页面未解锁时全部弹暗号框，首页和关于页不弹
- ✅ 解锁流程 —— 输对暗号后放行，跨页面保持，`search.xml` 成功解密出 16 篇文章
- ✅ 未解锁时搜索 —— 抛 `SEARCH_LOCKED` 并说清原因，而不是含糊地说「没搜到」
- ✅ 板块内容 —— 课外 3 门课、课内 4 门课、GAMES101 下 8 篇、Looman 下 6 篇
- ✅ 策划室 —— 项目列表、文档列表、markdown 渲染（表格 / 代码块 / 键值条目 / 有序列表编号）
- ✅ 反馈闭环 —— 提交后确实 PUT 回私有仓库，带上项目、文档、评价、未处理标记
- ✅ **站内死链** —— 5326 个链接全部解析得到，零死链
- ✅ **旧地址跳转** —— 9 条（6 个板块 + 2 个分类 + 早先改名的文章）全部落到正确的新地址
- ✅ 全流程零 JS 报错

**没验证的**（需要真实凭据，只能你自己跑一次）：

- 策划室连真实的 Claude API 和私有仓库跑完整一轮 —— **第一次请勾上 `dry` 演练**
- 娜娜莉的对话（需要 DeepSeek key 在浏览器保险箱里）
- 日报发信

---

## 八、部署之前

```bash
npm ci
npm test
NOIMPTY_PASSPHRASE="你的暗号" npm run build   # 本地想试的话
```

然后 push 到 `main`，`pages.yml` 会自动跑测试 → 构建 → 上锁自检 → 部署。
自检不过会直接失败，不会把一个敞开的站发出去。

**部署之后第一件事**：打开站点，确认三个入口都弹暗号框，输一次暗号能进去，
然后在任意页面问娜娜莉「今天几号」和「博客里有几篇文章」—— 这两条是这次修的重点。
